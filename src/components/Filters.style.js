import styled from 'styled-components';

export const StyledFilters = styled.div`
  display: flex;
  column-gap: 1rem;
  padding: 0.75rem 0;
  align-items: center;

  button {
    margin-left: auto;
  }
`;
